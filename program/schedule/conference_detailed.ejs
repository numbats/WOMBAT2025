```{=html}
<div style="display: flex; flex-direction: column; gap: 1.5rem; padding: 1rem; font-family: sans-serif;">
  <% for (const item of items) { %>
    <div style="border-left: 4px solid #007ACC; padding-left: 1rem;">
      <div style="font-size: 0.9rem; color: #555; font-weight: 500;"><%= item.time %></div>
      <div style="font-size: 1.1rem; font-weight: bold; margin: 0.25rem 0;"><%= item.talk %></div>
      
      <% if (item.chair) { %>
        <div style="font-size: 0.85rem; color: #666; margin-bottom: 0.5rem;">
          <strong>Chair:</strong> <%= item.chair %>
        </div>
      <% } %>

      <% if (item.details && item.details.length > 0) { %>
        <div style="display: flex; flex-direction: column; gap: 1rem; margin-top: 0.5rem;">
          <% for (const detail of item.details) { %>
            <div style="padding: 0.75rem; border: 1px solid #e0e0e0; border-radius: 8px; background-color: #fafafa;">
              <% if (detail.title) { %>
                <div style="font-weight: 600; font-size: 1rem;"><%= detail.title %></div>
              <% } %>
              <% if (detail.speaker) { %>
                <div style="font-size: 0.9rem; color: #444;"><strong>Speaker:</strong> <%= detail.speaker %></div>
              <% } %>
              <% if (detail.abstract) { %>
                <div style="font-size: 0.85rem; color: #333; margin-top: 0.5rem; white-space: pre-wrap;"><%= detail.abstract %></div>
              <% } %>
            </div>
          <% } %>
        </div>
      <% } %>
    </div>
  <% } %>
</div>
```
